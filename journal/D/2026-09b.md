# Line D journal — 2026-09 (continued)

Continues `2026-09.md`, which reached its line budget. Append-only narrative, never read at
session start; the handoff is the `## NEXT` block of `lines/D/STATE.md`.

## 2026-09-23 — corpus schema 3 (`pilot-v3-constco2`) and mid-tier readiness (branch `int/corpus`)

**Schema 3 exists and nothing pinned moved.** `vegemu.corpus.schema`: 2 = every table so far, 3 =
NaN tree-type shares on a treeless row plus the five soil columns (`vegemu.corpus.soil`). The
library DEFAULT stays 2, because `exp_derive_nulls_pilot.py` and `corpus_build.py` re-decode pinned
versions without passing anything. Proven by round-trip, not argued: schema-2 re-decode of v2 →
`9c117cb6…` byte-identical (job 2281353); `exp_derive_nulls_pilot.decode` unmodified →
`state_pilot-v2-constco2.parquet` `ea4ebee7…` byte-identical (job 2281356).

**`pilot-v3-constco2` = v2's 6,000 restarts re-decoded, no spin-ups** (`--out-version`, job
2281352): 6,000 × 186, sha `af6425f2…`; 174/181 shared columns identical, `pft_frac_*` differ on
exactly the 380 treeless rows; the soil columns equal `exp_equilibrium_map`'s read-time join
bitwise on all 6,000 rows. Provenance carries v2's plan hash and a per-restart sha256 manifest.
Second seed, once the integrator's spin-ups finish (in-place schema-2 decode FIRST, so the v3
decode has a source table to diff against):
`NCPUS=16 PARTITION=priority TIME=00:40:00 scripts/sbatch_py.sh D-pilot-v2-s2full-decode scripts/corpus_pilot.py --stage decode --version v2-constco2 --seed 2 --workers 16`
`NCPUS=16 PARTITION=priority TIME=00:40:00 scripts/sbatch_py.sh D-pilot-v3-s2full-decode scripts/corpus_pilot.py --stage decode --version v2-constco2 --seed 2 --out-version v3-constco2 --workers 16`

⚠ **Soil code 13 is "rock and ice"** in the ground truth's own `soilmap`, not the "clay (light)"
`screen_d95max.SOILPAR` transcribes. 3,180 grid cells carry it; **none of the 56,986 tree-bearing
cells does**, so no pilot, sealed or mid-tier number is touched. And `soil_awc` is METRES of water
(soil depth is in m), not mm as the inbound said.

**Mid tier planned as a DRY RUN only** (`--tier mid --version dryrun`, jobs 2281357/2281368):
1,000 cells, 164/164 tiles, cap 15, all 200 pilot cells, the pilot's 30 climates reproduced
exactly inside a 100-point nested design, 400 manifests per seed. A fixed cap of 3 really does top
out at 472 (now a refusal, real-data test). Pilot re-plan: `design_sha256` = `bad787ad…`, so the
pilot selection is unchanged. Projection per seed: 6,000 CPU core-hours, ~11,400 billed unpacked,
217 GB runs + 21.9 GB forcing (seed 1 only).
