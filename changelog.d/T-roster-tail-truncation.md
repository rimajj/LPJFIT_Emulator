### Fixed

- **The synthesised roster contained no tree above 13.3 m anywhere in the block, while the truth reaches 25.8 m — and those missing tall trees were the entire leaf-area shortfall.** The predicted quantiles are CELL quantiles (`corpus/state.py` pools all 25 patches before taking a percentile), but the roster was drawn inside each patch from `u = (arange(n) + 0.5) / n` with n ≈ 19. Every patch therefore received the same nineteen ranks, spanning 0.026 to 0.974, and the cell received twenty-five copies of one truncated ladder. A stand's tallest tree lives near rank 0.998, so it was unaddressable in every cell, however good the prediction — the donor pool held 794 admissible stems above 16 m reaching 30.3 m, and the predicted 90th-percentile height was good to 7 %. The true state holds 308 stems in 16–20 m and 72 in 20–25 m; the synthesised file held none, and those two empty classes account for 109 % of the leaf gap. Ranks are now drawn once across the cell and dealt to patches at random, so each patch is a random SAMPLE of the stand rather than a copy of it.
- **And three knots joined by straight lines cannot make a forest.** Cell-level ranks alone recover almost nothing: continuing the p50→p90 slope past the top knot reaches 14.3 m at rank 0.999, still 9 m short of the real tallest tree. The distributional SHAPE now comes from the template cell's own stems, recalibrated onto the three predicted knots, so only location and spread are the emulator's. This extends the mechanism `type_ladder` already used for tree type to the matched traits — one real stem per rank, read whole, so the template's joint height–density structure survives instead of two marginals being recombined at perfect rank correlation.
- **The previous record's proposed fix is rejected, on the model's own source.** It was to rescale each stem's leaf carbon to the predicted cell total, on the reasoning that leaf carbon is an untargeted per-stem mass. It is not: `tree/allometry_tree.c:39-41` derives height from it as `k_latosa * sapwood / (leaf * sla * wooddens)`, so multiplying leaf carbon by the 1.41 the prediction asked for would have divided every tree's height by 1.41 at the first allocation.

### Changed

- **Leaf area 15 % → 85 % of cells inside the acceptance band at twenty years, against the 90 % a real run attains**, with above-ground biomass 55 % → 75 % (the ceiling is 75 %), vegetation carbon 60 % → 85 %, median height 50 % → 75 %, wood density 70 % → 95 % and stems per patch 25 % → 50 %. **The drift also reverses direction:** the per-cell median vegetation-carbon gap used to GROW over the twenty years, 0.087 → 0.140, against the two control seeds' own 0.010 → 0.097; it now SHRINKS, 0.189 → 0.078, ending inside the controls' own spread. Basis: 20 cells of 54,020, temperate Europe, present-day climate, one task per arm — **not** the acceptance test.
- **It is still a fail, and by how much is the point.** 5 % of cells inside the band on all 22 quantities at once — one cell of twenty — against an attainable 25 %, with a median of 18 of 22 that exactly ties the "twenty years change nothing" null. The null still wins on rooting-depth low tail (100 % against 40 %) and soil carbon. What changed is that the emulator now decisively beats that null on the stocks, where before it lost to it on 14 of the 22.
- **Outside the outer knots the recalibration is multiplicative rather than extrapolated, and that was measured rather than chosen.** Continuing the interior slope amplified the level model's 5 % error at the 90th percentile into 13 % at the 99.9th, and since stem mass climbs steeply with height it put above-ground biomass 21 % over the truth while every other quantity improved. Anchoring the tail at its knot caps the distortion at the prediction's own error and is exactly the identity when the prediction is right; it alone moved the year-0 biomass error from 0.320 to 0.233.
- **The binding constraint has moved off the synthesiser and onto the level model.** Given the TRUE cell quantiles instead of the predicted ones, the synthesiser reproduces the real 1999 state to a median 0.4 % on above-ground biomass, 0.2 % on median and 90th-percentile height, 0.3 % on stems per patch and 2.1 % on leaf area. The only quantities it still misses under a perfect prediction are the traits the transplant does not match — rooting depth above all, at 12.7 % and 14.6 %. That oracle arm has seen the answer and can never be quoted as emulator skill; it is a bound on the mechanism.

### Added

- **`tests/test_synth_size_distribution.py`** — assertions about the TAILS and about the CELL, deliberately not about a median, because a median is exactly what stayed correct while this was broken. Pins the rank arithmetic (the top per-patch rank indexes below the cell's tallest rung), that the recalibration is the identity when the prediction is right, and that the tail never amplifies the prediction's own error.
- **`SynthReport` now reports the 90th-percentile shortfall per matched trait, the tallest stem placed, and the tallest the template holds** — the numbers that would have made this visible. It also records, per trait, whether the shape came from the template or from the three-knot fallback, so a cell that quietly took the short-tailed fallback cannot look like a cell that had a tail.
