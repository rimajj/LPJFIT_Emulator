### Added

- **The emitted-restart nulls, re-derived on a spatially dispersed cell set** (`scripts/exp_derive_nulls_restart_pilot.py`). 200 pilot cells across 164 populated 15° tiles and 5,800 (cell, climate) targets, against the 20 contiguous cells of the first derivation. Six nulls, each a real loadable restart you could hand the model instead: the cell's own unedited forest, the nearest climate analogue searched over the whole perturbed ensemble, the same search at one design point, the nearest cell, the average forest, and a random one. Nulls are out of fold on whole tiles, and a held-out cell is held out at all 30 of its climates so no arm can copy the answer off the same forest under a slightly different climate.
- **A band-floor sweep as a named diagnostic**, so "is the statistic pinned because the state is wrong or because the tolerance is too tight?" is answerable with a number instead of an argument. It does not move the 10 % acceptance floor and every wider floor is labelled as not an acceptance number, in the output key itself.

### Changed

- **The reason X4 is unsealed is now a different reason, and it is measured.** The 2026-09-09 record blamed the 20 contiguous cells; supplying 164 tiles did not fix it. The nulls collapse again — at the floor this time rather than the ceiling — with a **shuffled cell tying the best informed null at 0.004138** and the cell's own forest ranking below chance, smallest adjacent gap exactly 0.0. The cause is the band: from a 0.15 floor upward the ranking snaps into the order physics predicts and holds at every wider floor, and only at the 10 % acceptance floor is it scrambled. The acceptance tolerance is `max(10 %, the model's own two-seed spread)`, its median on these cells is exactly 0.100, and **the spread of a perturbed forest has never been measured** — so one unmade measurement decides the experiment.
- **Line D's second pilot seed (~34 core-hours) now discharges five asks rather than four** and is the highest-value job open. If the measured perturbed spread lands near 0.29, X4 seals as designed at a bar of 0.086 against an attainable 0.895; if near 0.10, the conjunctive level statistic is the wrong instrument and X4 needs a new estimand rather than a new corpus. Both branches are written down before the number exists.
- **A caution that reaches past X4:** one *real* realisation of LPJmL-FIT passes this conjunctive 22-quantity test in only **47 %** of its own cells at present-day climate on a transferred band. The project's acceptance statistic is severe for the model being emulated, not only for an emulator of it, so every conjunctive number quoted should carry its ceiling.
